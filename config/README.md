# config/

Place provider-specific config files here (service account JSON, AWS credential
files, etc.).

**All files in this directory except this README are gitignored.**
Never commit secrets or credentials.
