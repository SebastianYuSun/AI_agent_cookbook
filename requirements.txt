# All dependencies (runtime + dev)
litellm>=1.40.0
openai>=1.30.0        # required by litellm internally
boto3>=1.34.0         # required for AWS Bedrock
python-dotenv>=1.0.0

pytest>=8.0.0
pytest-cov>=5.0.0
black>=24.0.0
ruff>=0.4.0
